# Environment for the whole user session: both units, and niri's `qs ipc`
# keybinds. Installed by `make install-shell`, path fixed up for its PREFIX.
QS_CONFIG_PATH=/usr/share/thomas-shell/shell
# Same fixed cursor as niri's cursor {} block and greeter/config.toml.
XCURSOR_THEME=Adwaita
XCURSOR_SIZE=24
